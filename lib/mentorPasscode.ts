/**
 * Shared convenience gate for mentor/QA-only tools (demo auto-fill, answer-key
 * generation). Not real security — the code lives in client-side plaintext —
 * just a guard against accidental clicks by a learner.
 */
export const MENTOR_PASSCODE = "muchson123";
